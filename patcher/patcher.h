#define TX_USE_SPEAK
#include "TXLib.h"
#include "text-sort.h"

int BinPatch(char* name);

HDC& InitialPrep();

int PatcherMain(char* name);

HDC LoadImg(char name[]);

int DrawProgressBar();

int EndPatch();

